<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Submission Result</title>
    <style>
        /* General reset */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        /* Body styling */
        body {
            font-family: 'Arial', sans-serif;
            display: flex;
            align-items: center;
            justify-content: center;
            flex-direction: column;
            min-height: 100vh;
            background: linear-gradient(135deg, #184859, #c26aa0);
            color: #fff;
            text-align: center;
        }

        /* Header styling */
        h1 {
            font-size: 2.5rem;
            color: #f8f9fa;
            margin-bottom: 20px;
            text-shadow: 2px 2px 5px rgba(0, 0, 0, 0.3);
        }

        /* Paragraph styling */
        p {
            font-size: 1.2rem;
            line-height: 1.5;
            max-width: 600px;
            margin: 0 auto;
            color: #e8e9eb;
        }

        /* Responsive adjustments */
        @media (max-width: 768px) {
            h1 {
                font-size: 2rem;
            }
            p {
                font-size: 1rem;
            }
        }
    </style>
</head>
<body>
    <h1>Thank you for visiting us, <%= name %>!</h1>
    <p>We received your submission and will get back to you shortly. Have a great day!</p>
</body>
</html>
